{
  "name": "nanojournal",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp public/* dist/",
    "dev": "concurrently 'tsc -w && cp public/* dist/' 'cd dist/ && live-server --watch=dist,index.html'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.1",
    "live-server": "^1.2.2",
    "typescript": "^5.6.3"
  }
}